# Run.ps1 Functional Requirements

**Reminder:** If any new permanent functionality is added to Run.ps1, update this requirements file to reflect the changes.

- Runs a script from the `scripts` folder by name, passing any additional arguments.
- Used as a wrapper to standardize script execution.
- Must run the specified script from the `scripts` folder with any additional arguments.
- Must fail if the script does not exist.
